version: "2"
run:
  timeout: 10m
  build-tags: [ego_mock_eclient]

linters:
  exclusions:
    presets: [std-error-handling]
    rules:
    - linters: ["staticcheck"]
      text: "QF1001: could apply De Morgan's law"

formatters:
  enable: [gofumpt]
